import React from 'react';
import Form from "../Form";
import Menu from "../munu/Menu";

const HomePage = () => {
    return (
        <div>
            <Menu/>
        </div>
    );
};

export default HomePage;